import { StoryObj, Meta } from '@storybook/react';
import Component from './read-skeleton';

const meta: Meta<typeof Component> = {
  component: Component,
}

export default meta;
type Story = StoryObj<typeof Component>;

export const Primary: Story = {

}